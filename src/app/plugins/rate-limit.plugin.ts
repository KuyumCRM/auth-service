// Redis-backed rate limiter — placeholder.
import type { FastifyInstance } from 'fastify';

export async function rateLimitPlugin(_app: FastifyInstance): Promise<void> {
  // Plugin registration only; no logic.
}
