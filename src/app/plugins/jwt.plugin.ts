// RS256 JWT sign/verify plugin — placeholder.
import type { FastifyInstance } from 'fastify';

export async function jwtPlugin(_app: FastifyInstance): Promise<void> {
  // Plugin registration only; no logic.
}
