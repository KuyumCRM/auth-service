#!/usr/bin/env node
/**
 * RSA keypair generation stub. Generates RS256 keypair for JWT.
 * Real implementation will output keys to .env or stdout.
 */
console.log('Generate RS256 keypair and set JWT_PRIVATE_KEY_B64, JWT_PUBLIC_KEY_B64 in .env (stub)');
process.exit(0);
